#### Adserv Daemon Bindings
# port and certificate are option
# ip:port:/path/to/certandkey.pem
#   ip = IPv4 or IPv6 address if you use [::]
#   port = port number, default 80 for http and 443 for https
#   cert = path to certificate and unencrypted private key in pem format
#
# examples:
# 127.0.0.1
# 127.0.0.1:8080
# 127.0.0.1:/etc/ssl/goto.pem
# 127.0.0.1:8443:/etc/ssl/goto.pem
# [::1]
# [::1]:8080
# [::1]:/etc/ssl/goto.pem
# [::1]:8443:/etc/ssl/goto.pem
#
# listen on all IPv4 address on port 8080
0.0.0.0:80
