## interface configuration
# change listen-address to the correct IP, it is used by adblock-updater
listen-address=127.0.0.1
bind-interfaces

## dns configuration
no-hosts
domain=local
cache-size=1500

resolv-file={{SYSCONFDIR}}/adblock/adblock-resolv.conf
conf-file={{SYSCONFDIR}}/adblock/adblock-blacklist.conf
